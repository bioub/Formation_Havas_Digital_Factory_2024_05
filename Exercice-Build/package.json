{
  "devDependencies": {
    "http-server": "^13.1.0",
    "terser": "^5.31.0"
  },
  "scripts": {
    "serve:dev": "http-server src",
    "serve:prod": "http-server dist",
    "build": "node build.mjs --minify --hash"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
